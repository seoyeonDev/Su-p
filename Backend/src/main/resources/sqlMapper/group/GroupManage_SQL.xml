<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="com.example.studyproject.group.GroupDao">

	<!-- 그룹 추가 -->
	<insert id="createGroup">
		INSERT INTO group
		(
			group_id
			leader_id
			title
			name
			desc
			kind
			status
			mem_cnt
			startdate
			enddate
			chk_m
			chk_d
		)
		VALUES
		(
			#{group_id},
			#{leader_id},
			#{title},
			#{name},
			#{desc},
			#{kind},
			#{status},
			#{mem_cnt},
			#{startdate},
			#{enddate},
			#{chk_m},
			#{chk_d}
		);
	</insert>



	<delete id="deleteGroup">
		DELETE
		FROM studygroup
		WHERE
			group_id = #{group_id}
		AND
			leader_id = #{leader_id}
	</delete>
</mapper>